<UserControl
    x:Class="DiktaMe.App.Views.TrayIconView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- TaskbarIcon is created entirely in code-behind to avoid
         XAML rendering / x:Bind issues when hosted outside a visual tree. -->
</UserControl>
